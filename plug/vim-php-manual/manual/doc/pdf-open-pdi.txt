PDF_open_pdi

(PHP 4 >= 4.0.5, PECL pdflib >= 1.0.0)

PDF_open_pdi — Open PDF file [deprecated]

==============================================================================
*Description*

    int PDF_open_pdi ( resource $pdfdoc , string $filename , string $optlist~
    , int $len )~

Opens a disk-based or virtual PDF document and prepares it for later use.

This function is deprecated since PDFlib version 7, use |PDF_open_pdi_document
| instead.


vim:ft=help: